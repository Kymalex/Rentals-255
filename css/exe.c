#include <stdio.h>
main()
{
	int a, b, result;
	printf("Enter the first number: \n");
	scanf("%d", & a);
	printf("Enter the second number: \n");
	scanf("%d", & b);
	dividing = a / b ;
	result = (a/b);
	printf("result")

	if (a ==0 or b == 0)
	{
		return dividing;
	}
}